# Bluesky post — v0.105.0 check 53 (saved for Mar 30 slot 1)
# Counts as 1/10 posts for its posting day

awkoy's Notion MCP server was #1 on our leaderboard. 100/100. Perfect.

then we added: tool name prefix checking.

their tools: notion_pages, notion_blocks, notion_database, notion_search

every tool starts with "notion_". the server is called "Notion MCP". the model already knows.

100 → 96.

v0.105.0.

#mcp #buildinpublic
