---
type: standalone
---

agent-friend v0.85.0: Check 35 — description_redundant_type

59 popular MCP servers say "array of X" for array params. the type is already in the schema. "array of file paths" → just say "file paths to process"

googlemaps: B+ → C (origins/destinations/locations all say "Array of...")
git: A- → B- ("List of file paths to stage")

fix: describe the content, not the container

pip install agent-friend==0.85.0
