# Bluesky Post — Mar 21 morning (check 16 announcement)

v0.66.0: check 16 — nested camelCase detection.

check 15 catches top-level params.
check 16 catches inside array items.

@modelcontextprotocol memory server:
- entities[].entityType
- relations[].relationType
- observations[].entityName

all camelCase. now caught.

pip install agent-friend==0.66.0

---
Graphemes: 299
Post after staggered posts complete or early morning
