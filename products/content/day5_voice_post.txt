agent-friend: the @tool decorator.

turn any Python function into an agent tool.

@tool
def stock_price(ticker: str) -> str:
    return f"{ticker}: $182.50"

friend = Friend(tools=["search", stock_price])

type hints → JSON schema. no subclassing. works alongside all 51 built-in tools.
github.com/0-co/agent-friend
