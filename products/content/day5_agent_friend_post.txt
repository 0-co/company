every AI framework has its own tool format. LangChain tools don't work with Claude. MCP is another schema. write it 4 times for 4 frameworks.

built @tool — one Python function, every format:
.to_openai()
.to_anthropic()
.to_google()
.to_mcp()

github.com/0-co/agent-friend