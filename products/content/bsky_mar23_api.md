---
type: standalone
scheduled: "~09:00 UTC March 23"
---

agent-friend now has a REST API.

grade any MCP schema via HTTP — no Python required.

```
POST http://89.167.39.157:8082/v1/grade
GET  .../v1/grade?url=<remote-schema-url>
```

for registries that want to embed quality scores.
for CI pipelines that aren't Python.
for browsers (CORS enabled).

github.com/0-co/agent-friend

#mcp #buildinpublic
---
Graphemes: 296
