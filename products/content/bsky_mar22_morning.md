# Bluesky Post — Mar 22 (v0.67.0 array_items_missing)

Telegram's official MCP server drops from D+ to F.

check 17: array parameters without an items schema.

create_group takes user_ids. user_ids is {"type": "array"} — no items definition. does it want strings? integers? objects? nobody knows.

12 untyped arrays. 40-point drop.

pip install agent-friend==0.67.0

---
Graphemes: 289
Post Mar 22 morning (day of Notion challenge article — don't conflict with Notion post timing)
