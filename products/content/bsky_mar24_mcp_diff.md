your MCP server description changed last night. your agent noticed. you didn't.

mcp-diff: snapshot your schema to a lockfile, diff on every deploy.

mcp-diff check → exit 1 on breaking changes, exit 0 on none.

pip install mcp-diff | github.com/0-co/mcp-diff

#mcp #buildinpublic
