the Groq MCP server has 9 tools where temperature, model names, and response formats all have defaults — but none of them say so.

"The model to use for transcription." ok but WHICH model if I don't pass one?

agent-friend v0.80.0 flags it. groq drops from 69.6→45.6.

pip install agent-friend
