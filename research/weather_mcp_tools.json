[
  {"name": "get_current_weather", "description": "Get current weather conditions for any location", "inputSchema": {"type": "object", "properties": {"location": {"type": "string", "description": "City name or coordinates (lat,lon)"}, "units": {"type": "string", "enum": ["metric", "imperial", "standard"], "description": "Temperature unit system"}, "include_details": {"type": "boolean", "description": "Include extended information"}}, "required": ["location"]}},
  {"name": "get_forecast", "description": "Get 5-day weather forecast with 3-hour intervals", "inputSchema": {"type": "object", "properties": {"location": {"type": "string", "description": "City name or coordinates"}, "days": {"type": "integer", "minimum": 1, "maximum": 5, "description": "Number of forecast days"}, "units": {"type": "string", "enum": ["metric", "imperial", "standard"]}}, "required": ["location"]}},
  {"name": "search_location", "description": "Find locations and their coordinates by name", "inputSchema": {"type": "object", "properties": {"query": {"type": "string", "description": "Location name to search"}, "limit": {"type": "integer", "minimum": 1, "maximum": 5, "description": "Maximum results returned"}}, "required": ["query"]}},
  {"name": "get_weather_by_zip", "description": "Get weather by ZIP or postal code", "inputSchema": {"type": "object", "properties": {"zip_code": {"type": "string", "description": "ZIP or postal code"}, "country_code": {"type": "string", "description": "ISO country code (e.g., US, GB)"}, "units": {"type": "string", "enum": ["metric", "imperial", "standard"]}}, "required": ["zip_code"]}},
  {"name": "get_air_quality", "description": "Get air pollution data and AQI for a location", "inputSchema": {"type": "object", "properties": {"location": {"type": "string", "description": "Coordinates or city name"}}, "required": ["location"]}}
]
