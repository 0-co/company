[
  {"name": "create_mount", "description": "Establishes a new mount in Vault for secret storage", "inputSchema": {"type": "object", "properties": {"type": {"type": "string", "description": "Mount category (kv, kv2, pki)"}, "path": {"type": "string", "description": "Mount location path"}, "description": {"type": "string", "description": "Optional mount details"}}}},
  {"name": "list_mounts", "description": "Retrieves all configured mounts in Vault", "inputSchema": {"type": "object", "properties": {}}},
  {"name": "delete_mount", "description": "Removes a mount from Vault", "inputSchema": {"type": "object", "properties": {"path": {"type": "string", "description": "Mount location to remove"}}, "required": ["path"]}},
  {"name": "list_secrets", "description": "Enumerates secrets within KV mount paths", "inputSchema": {"type": "object", "properties": {"mount": {"type": "string", "description": "KV engine mount path"}, "path": {"type": "string", "description": "Optional secret directory path"}}, "required": ["mount"]}},
  {"name": "read_secret", "description": "Retrieves secrets from KV mount locations", "inputSchema": {"type": "object", "properties": {"mount": {"type": "string", "description": "KV engine mount path"}, "path": {"type": "string", "description": "Full secret retrieval path"}}, "required": ["mount", "path"]}},
  {"name": "write_secret", "description": "Stores secrets in KV mount locations", "inputSchema": {"type": "object", "properties": {"mount": {"type": "string", "description": "KV engine mount path"}, "path": {"type": "string", "description": "Full secret storage path"}, "key": {"type": "string", "description": "Secret identifier"}, "value": {"type": "string", "description": "Secret content"}}, "required": ["mount", "path", "key", "value"]}},
  {"name": "delete_secret", "description": "Removes secrets or individual keys from KV storage", "inputSchema": {"type": "object", "properties": {"mount": {"type": "string", "description": "KV engine mount path"}, "path": {"type": "string", "description": "Secret location"}, "key": {"type": "string", "description": "Optional specific key to remove"}}, "required": ["mount", "path"]}},
  {"name": "issue_pki_certificate", "description": "Generates new certificates using PKI roles", "inputSchema": {"type": "object", "properties": {"mount": {"type": "string", "description": "PKI engine mount path"}, "role": {"type": "string", "description": "Role identifier for issuance"}, "commonName": {"type": "string", "description": "Certificate CN value"}, "altNames": {"type": "string", "description": "Optional alternative certificate names"}, "ttl": {"type": "string", "description": "Optional certificate validity period"}}, "required": ["mount", "role", "commonName"]}}
]
